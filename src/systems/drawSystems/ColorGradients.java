package systems.drawSystems;

import util.geometry.Polygon;

public class ColorGradients extends Gradients {

    public ColorGradients(Edge tallestEdge, Edge bottomEdge, Edge topEdge, Polygon p) {
        super(tallestEdge, bottomEdge, topEdge, p);
    }


}
